# The kernel reads a script's shebang literally. A CRLF ending turns
# "#!/bin/sh" into a request for "/bin/sh\r", which does not exist, and the
backend/hook/bin/*       text eol=lf
*.sh                     text eol=lf
Dockerfile*              text eol=lf
