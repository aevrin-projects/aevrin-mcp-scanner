"""Business logic: quota, scanning, triage, auto-fix, reports, admin auth.

This is where the product rules live. Services may use core/ and lib/, and are
called by routes/ - never the other way round.
"""
